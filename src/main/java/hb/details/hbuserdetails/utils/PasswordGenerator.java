package hb.details.hbuserdetails.utils;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;

public class PasswordGenerator {
	/*
	 * public static void main(String[] args) { BCryptPasswordEncoder b = new
	 * BCryptPasswordEncoder(); //BCrypt.gensalt(null, 0); String pass =
	 * b.encode("prakash123"); System.out.println(pass); }
	 */
}
